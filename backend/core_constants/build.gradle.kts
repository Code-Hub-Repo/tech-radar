// core_constants — foundational module, no project dependencies.
// Houses AppConfig, LogTags, ApiRoutes, ErrorCodes, ValidationConstants and other shared constants.
// Kotlin JVM plugin, toolchain, and JUnit platform are applied globally via the root build.gradle.kts.
